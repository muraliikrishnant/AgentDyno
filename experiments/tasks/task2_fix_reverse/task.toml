schema_version = "1.4"

[task]
name = "agentdyno/fix-reverse"
version = "1.0.0"
description = "Repair the string reversal function so the provided regression tests pass."
keywords = ["programming", "python", "bug-fix"]

[metadata]
category = "programming"
difficulty = "easy"

[verifier]
timeout_sec = 60.0

[agent]
timeout_sec = 600.0

[environment]
docker_image = "python:3.12-slim"
workdir = "/app"
cpus = 1
memory_mb = 1024
